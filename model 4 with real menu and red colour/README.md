# রেড ফক্স (Red Fox) - ডিজিটাল মেনু

একটি আধুনিক, মোবাইল-ফ্রেন্ডলি রেস্টুরেন্ট মেনু ওয়েবসাইট।

## ফাইল স্ট্রাকচার

```
red-fox-menu/
│
├── index.html          # মূল HTML ফাইল
├── style.css           # সকল CSS স্টাইল
├── script.js           # সকল JavaScript কোড
└── README.md           # এই ফাইল
```

## কিভাবে ব্যবহার করবেন

1. সকল ফাইল একই ফোল্ডারে রাখুন
2. `index.html` ফাইলটি ব্রাউজারে খুলুন
3. এখনই আপনার মেনু দেখতে পাবেন!

## বৈশিষ্ট্যসমূহ

✨ **ডিজাইন:**
- সুন্দর বাংলা ফন্ট
- ডার্ক ও লাইট মোড
- মোবাইল রেসপন্সিভ
- স্মুথ অ্যানিমেশন

🍔 **মেনু ক্যাটাগরি:**
- **Burger & Snacks** (১৯টি আইটেম) - Student Burger, Dragon Burger, Jumbo Burger, French Fries etc.
- **Drinks & Shakes** (১৪টি আইটেম) - Chocolate Cold Coffee, Strawberry Shake, Oreo Shake etc.
- **Sandwich & Shawarma** (৯টি আইটেম) - Chicken Shawarma, Sub Sandwich, BBQ Sub etc.
- **জুস (Juice)** (১১টি আইটেম) - ফালুদা, মাল্টা জুস, ম্যাংগো জুস etc.
- **Pizza** (৭টি আইটেম) - Margherita, Pollo, Mix Pizza etc.
- **ফুসকা ও চটপটি** (৭টি আইটেম) - দেশী ফুসকা, দই ফুসকা, চটপটি etc.

📞 **যোগাযোগ:**
- কল করার বাটন
- হোয়াটসঅ্যাপ বাটন
- গুগল ম্যাপ লিংক

## কাস্টমাইজেশন

### রেস্টুরেন্টের নাম পরিবর্তন করতে:
`index.html` ফাইলে "মামা রেস্টুরেন্ট" খুঁজুন এবং আপনার নাম দিন।

### ফোন নম্বর পরিবর্তন করতে:
`index.html` ফাইলে নিচের লাইনগুলি খুঁজুন এবং পরিবর্তন করুন:

```html
<a href="tel:01700000000">  <!-- এখানে আপনার নম্বর দিন -->
<a href="https://wa.me/8801700000000">  <!-- এখানে হোয়াটসঅ্যাপ নম্বর দিন -->
```

### মেনু আইটেম যোগ/পরিবর্তন করতে:
`script.js` ফাইলে `menuData` অবজেক্টে যান এবং আপনার আইটেম যোগ করুন:

```javascript
burgers: [
    { 
        name: 'আইটেমের নাম', 
        price: '৫০', 
        desc: 'বিবরণ', 
        image: 'ছবির URL',
        popular: true  // জনপ্রিয় ব্যাজ দেখাতে
    }
]
```

### নতুন ক্যাটাগরি যোগ করতে:
`script.js` ফাইলে `categories` অ্যারেতে নতুন ক্যাটাগরি যোগ করুন:

```javascript
{ 
    id: 'desserts', 
    name: 'ডেজার্ট', 
    icon: 'cake',  // Lucide icon name
    gradient: 'bg-gradient-to-br from-pink-400 to-rose-500',
    count: 5
}
```

তারপর `menuData` তে ঐ ক্যাটাগরির আইটেম যোগ করুন।

### রঙ পরিবর্তন করতে:
`style.css` ফাইলে প্রাইমারি কালার পরিবর্তন করুন অথবা `index.html` এর Tailwind config এ:

```javascript
colors: { 
    primary: '#f97316',    // আপনার পছন্দের রঙ কোড
    secondary: '#fb923c'
}
```

## প্রয়োজনীয় লাইব্রেরি

এই প্রজেক্ট নিচের CDN লাইব্রেরিগুলি ব্যবহার করে (ইন্টারনেট সংযোগ প্রয়োজন):
- Tailwind CSS
- Lucide Icons
- Alpine.js
- Google Fonts (Noto Serif Bengali & Hind Siliguri)

## বিশেষ নোট

**আইসক্রিম ও কোল্ড ড্রিঙ্কস গায়ের মূল্যে (MRP) বিক্রয় করা হয়।**

## সাপোর্ট

কোনো সমস্যা হলে যোগাযোগ করুন!

---

© ২০২৬ রেড ফক্স (Red Fox)। সর্বস্বত্ব সংরক্ষিত।
